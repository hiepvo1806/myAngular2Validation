import { HvValidatorDirective } from './hv-validator.directive';

describe('HvValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HvValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
