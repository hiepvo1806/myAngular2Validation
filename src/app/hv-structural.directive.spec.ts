import { HvStructuralDirective } from './hv-structural.directive';

describe('HvStructuralDirective', () => {
  it('should create an instance', () => {
    const directive = new HvStructuralDirective();
    expect(directive).toBeTruthy();
  });
});
