import { SampleValidationDirective } from './sample-validation.directive';

describe('SampleValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new SampleValidationDirective();
    expect(directive).toBeTruthy();
  });
});
